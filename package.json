{
  "name": "@ethernick/statamic-activitypub-core",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build:v5": "cross-env BUILD_ADDON=true VUE_VERSION=2 vite build && mkdir -p dist/v5/css && cp resources/css/cp.css dist/v5/css/cp.css",
    "build:v6": "cross-env BUILD_ADDON=true VUE_VERSION=3 vite build && mkdir -p dist/v6/css && cp resources/css/cp.css dist/v6/css/cp.css",
    "build": "./bin/build-v6.sh && npm run build:v5",
    "production": "npm run build"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "@vitejs/plugin-vue2": "^2.3.4",
    "cross-env": "^7.0.3",
    "vite": "^7.0.7",
    "vite-plugin-externals": "^0.6.2"
  },
  "dependencies": {
    "vue": "^2.7.16"
  }
}